{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Docker: .NET Core Debug",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "netCore": {
                "appProject": "${workspaceFolder}/Rayson.Dev/Api/Presentation/Presentation.csproj"
            }
        }
    ]
}
